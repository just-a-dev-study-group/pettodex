/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'lkagfozeem7jfo5t.public.blob.vercel-storage.com',
                port: '',
            },
        ],
    },


};

export default nextConfig;
